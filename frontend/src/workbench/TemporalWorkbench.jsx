export function TemporalWorkbench({ children }) {
  return (
    <main aria-label="Temporal verification workbench">
      <header>
        <h1>Temporal verification workbench</h1>
        <p>Decision support, not authority.</p>
      </header>
      {children}
    </main>
  );
}
