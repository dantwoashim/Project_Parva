# Parva Conformance Suite

The conformance suite verifies local public preview compatibility with Parva Protocol.

Run:

```bash
python scripts/parva_conformance.py --target local --level parva_core
```

The public suite uses public fixtures and must not require private source archives.
