# Calendar Plugin Template

1. Rename `TemplateCalendarPlugin` and `plugin_id`.
2. Implement conversion/validation methods.
3. Add plugin corpus to `tests/fixtures/plugins/`.
4. Register plugin in `backend/app/engine/plugins/registry.py`.
