# Rules Layer

`backend/app/rules/` contains festival rule evaluation logic and adapters.

Responsibilities:
- Load normalized festival rules.
- Execute date calculations using the active engine.
- Provide a stable service interface for API routes.
