# Fiscal-Year Example

Use the public enterprise calendar surface for Nepali fiscal-year and business date validation where enabled.

```bash
curl https://api.prabinghimire1.com.np/v3/api/enterprise/capabilities
```

Private deployments can pin source policy and approval workflow for organization-specific fiscal use.
