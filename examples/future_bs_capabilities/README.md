# Future BS Capabilities Example

The public future-BS endpoint returns methodology and claim-boundary metadata only.

```bash
curl https://api.prabinghimire1.com.np/v4/api/future-bs/capabilities
```

It does not return direct future month-length values, full future vectors, exports, backtests, model runs, or corrected-value outputs.
