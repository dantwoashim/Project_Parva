# Benchmark Tasks

Task definitions for v0 are currently stored in `public-benchmark/benchmark.json`
so the benchmark is easy to validate as one JSON artifact. Future versions can
split each task into this directory when the task count grows.
