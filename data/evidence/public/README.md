# Public Evidence Data

This folder stores public-safe evidence examples and schemas. Evidence packets
record where a public source came from, what was extracted, how rows were
normalized, whether human review is still required, and whether the packet can
be promoted into a benchmark candidate.

Evidence packets do not create authority. They preserve source metadata and
review boundaries so reviewers can inspect the chain from source reference to
normalized row.

