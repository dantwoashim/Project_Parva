# Parva Protocol Registry

The registry directory documents public preview registry layout.

Subdirectories:

- `sources`
- `releases`
- `profiles`
- `rules`
- `credentials`
- `compatibility`
- `revocations`

Public registry data must not include private source archives, private credentials, customer dependencies, secrets, or local absolute paths.
