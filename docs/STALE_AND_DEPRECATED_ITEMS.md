# Stale And Deprecated Items

These items remain in the repo because deleting them blindly would be riskier than documenting them.

| Path or surface | Status | Notes |
| --- | --- | --- |
| `/api/*` | Legacy compatibility | Still served for existing clients, but not recommended for new integrations |
| `/v2`, `/v4`, `/v5` | Experimental aliases | Disabled by default and not real independent API versions |
| `scripts/telegram/bot_poc.py` | Unsupported PoC | Kept public as an example, not part of the supported product surface |
| `stitch/` | Design/reference artifacts | Useful for context, but not normative product docs |
| `docs/KNOWN_LIMITS.md` | Compatibility pointer | Retained so older links keep resolving |
