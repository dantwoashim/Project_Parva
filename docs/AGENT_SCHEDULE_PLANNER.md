# Agent Schedule Planner

The public preview planner supports payroll schedules using the `last_working_day_of_nepali_month` RuleLang rule.

Every item carries its RuleLang decision, fact IDs, warnings, and review status.

Private institution rules require private deployment configuration.
