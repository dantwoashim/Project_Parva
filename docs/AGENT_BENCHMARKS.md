# Agent Benchmarks

The public benchmark runner checks deterministic tool behavior.

It covers:

- date conversion claim verification
- false mapping rejection
- unsupported claim handling
- payroll schedule planning
- human-review gates
- impact tool integration

It does not call external AI services.
