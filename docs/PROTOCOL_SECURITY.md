# Protocol Security

Public protocol artifacts must not expose secrets, private source paths, private credentials, private customer dependencies, or fake signatures.

Preview credentials and offline bundles use hash verification and `unsigned_preview` or `hash_only_preview` labels.
