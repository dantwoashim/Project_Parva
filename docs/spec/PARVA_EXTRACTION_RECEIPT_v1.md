# Parva Extraction Receipt v1

An extraction receipt connects a normalized field to a source docket and source
region. It records extracted text, normalization rule, manual patch state,
normalized row hash, and review status.
