# Parva Transparency Log v1

The transparency log is append-only JSONL. Each entry commits to the previous
entry hash and its own canonical JSON hash. Forecast commitments remain
non-official until an external authority resolves them.
