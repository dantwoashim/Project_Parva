# Parva Embed Protocol v1

The embed protocol loads static Parva artifacts into a host page without
requiring filesystem writes, shell execution, or private routes. Embedded cards
must preserve boundary and verification status.
